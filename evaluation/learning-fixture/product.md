# 库存预留演示项目

本目录全部为开发者编写的合成资料，不是主办方数据，也不是历史人工 Session。

客户端提交 request_id、商品和数量。同一 request_id 重复到达时，应返回第一次预留的结果，不再扣减库存。相同 request_id 携带不同商品或数量时应拒绝请求，不能沿用旧结果。

本示例仅在单进程内演示状态变化。真实并发和进程重启需要数据库事务与唯一约束，不能由本示例测试推断已经满足。
